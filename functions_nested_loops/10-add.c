#include "main.h"
#include <stdio.h>

/**
 * add - Write a function that adds two integers and returns the result.
 * @a: first parametr.
 * @b: two parametr.
 * Return: Always 0.
 */

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
